// Compiler Warnings are issues within your program that could lead to bigger problems, but is still able to generate proper machine code

// preprocessor directives
#include <iostream>

// function main
int main() {
      // declare an integer variable
      int foo;

      // display to the user the integer in 'foo'
      std::cout << "Number is: " << foo;

      return 0;
}
